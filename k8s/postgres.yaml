apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres-deployment
  namespace: g1-a-order-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
        - name: postgres
          image: postgres:latest
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: order_service
            - name: POSTGRES_USER
              value: postgres
            - name: POSTGRES_PASSWORD
              value: "123"
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  namespace: g1-a-order-service
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
